%h1= 'Editing ' + @post.title

= semantic_form_for @post do |f|
  = f.inputs do
    = f.input :title
    = f.input :body
  = f.actions
= button_to "Destroy", @post, :confirm => "Are you sure you want to delete #{@post.title}?", :method => :delete
